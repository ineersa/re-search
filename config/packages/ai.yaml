ai:
    platform:
        # Inference Platform configuration
        # see https://github.com/symfony/ai/tree/main/src/platform#platform-bridges
        #
        # Platform selected via AI_PLATFORM env; ResearchPlatformFactory resolves
        # from container. llama=app.llama_cpp.platform, generic=ai.platform.generic.default

        # openai:
        #    api_key: '%env(OPENAI_API_KEY)%'

    agent:
        # web_research: thin agent for tool wiring; real orchestration in RunOrchestrator (Option B)
        # Agent configuration
        # see https://symfony.com/doc/current/ai/bundles/ai-bundle.html

        # default:
        #    platform: 'ai.platform.openai'
        #    model: 'gpt-5-mini'
        #    prompt: |
        #        You are a pirate and you write funny.
        #    tools:
        #        - 'Symfony\AI\Agent\Bridge\Clock\Clock'

    store:
        # Store configuration

        # chromadb:
        #    default:
        #        client: 'client.service.id'
        #        collection: 'my_collection'
