# Research submit throttling: IP + session identity — 10 requests per day
framework:
    rate_limiter:
        research_submit:
            policy: 'sliding_window'
            limit: 10
            interval: '1 day'
