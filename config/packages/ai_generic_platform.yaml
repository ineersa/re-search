ai:
    platform:
        generic:
            default:
                base_url: '%env(GENERIC_BASE_URL)%'
                api_key: null
                http_client: http_client
                model_catalog: ai.platform.model_catalog.generic
